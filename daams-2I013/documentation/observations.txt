PROBLEMES CONNUS:

1) la fonction intersection_polygones a le bon comportement general, mais le test semble trop approximatif
il y a des cas où la fonction detecte une collision alors qu'il n'y a rien sur l'affichage
ce probleme survient surtout au demarrage de la simulation et avec un grand nombre d'obstacle
Map builder semble aussi laisser passer des obstacles qui chevauchent le robot, donc on peut avoir des le debut
un obstacle sur le robot, meme si ce cas est plus rare

vu la rarete de ces evenements, il se peut que l'algorithme ne gere pas certains cas particuliers
on devra sans doute passer par un autre algorithme

2) confusion au niveau des coordonnees des points
la base du repere etant en haut a gauche dans tkinter, doit on quand meme determiner les points comme dans un repere normal ?
il est question ici d'avoir un affichage coherent

3) confusion au niveau de la structure du code, modifier quelque chose a un endroit n'est pas evident vu les implications des (nombreux) fichiers
un manuel.txt sera necessaire et urgent

4) pas tres sur si notre structure est bien une structure "MVC"

5) a cause de la facon dont on initialise certaines variables et la facon dont le code fonctionne, on ne peut pas appeler les fonctions dans un ordre au hasard dans le main
par exemple on doit build le monde seulement apres avoir creer au moins un robot
